{
  "buildCommand": "cd myApp && npm install && npm run build",
  "outputDirectory": "myApp/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
